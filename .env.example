DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
SECRET_KEY=change-me
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=5432

# Email: di default (lasciando questi vuoti) le email vengono solo stampate sulla
# console del server, comodo in sviluppo. Per spedirle davvero in produzione:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_BACKEND=
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=gestionale@maneggio.local
